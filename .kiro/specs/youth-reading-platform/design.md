# 技术设计文档

## 概述

青少年阅读平台是一个纯前端静态网站，使用原生 HTML、CSS 和 JavaScript 构建，部署在 Gitee Pages 上。该平台为青少年提供书籍、纪录片、电视剧、电影和少儿频道等多种内容类型的浏览和搜索功能。

### 核心设计原则

1. **纯静态架构**: 完全不依赖后端服务器，所有数据通过 JSON 文件存储
2. **渐进增强**: 基础功能在所有浏览器上可用，现代浏览器提供增强体验
3. **性能优先**: 优化加载速度，确保 3 秒内完成首次内容渲染
4. **响应式设计**: 适配桌面、平板、手机等不同设备
5. **无障碍访问**: 遵循 WCAG AA 标准，支持辅助技术

### 技术栈

- **HTML5**: 语义化标签，提供良好的结构和可访问性
- **CSS3**: 使用 Flexbox 和 Grid 实现响应式布局，CSS 变量管理主题
- **原生 JavaScript (ES6+)**: 实现交互逻辑、路由、搜索等功能
- **JSON**: 存储内容元数据和配置信息

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                              │
├─────────────────────────────────────────────────────────┤
│  HTML 页面层                                              │
│  ├─ index.html (首页)                                     │
│  ├─ category.html (分类列表页)                            │
│  ├─ detail.html (内容详情页)                              │
│  ├─ search.html (搜索结果页)                              │
│  └─ about.html (关于/帮助页)                              │
├─────────────────────────────────────────────────────────┤
│  JavaScript 应用层                                        │
│  ├─ router.js (前端路由)                                  │
│  ├─ dataLoader.js (数据加载)                              │
│  ├─ searchEngine.js (搜索引擎)                            │
│  ├─ renderer.js (内容渲染)                                │
│  └─ utils.js (工具函数)                                   │
├─────────────────────────────────────────────────────────┤
│  CSS 样式层                                               │
│  ├─ base.css (基础样式和变量)                             │
│  ├─ layout.css (布局和响应式)                             │
│  ├─ components.css (组件样式)                             │
│  └─ themes.css (主题配色)                                 │
├─────────────────────────────────────────────────────────┤
│  数据层 (静态 JSON 文件)                                  │
│  ├─ content.json (所有内容元数据)                         │
│  ├─ recommendations.json (推荐内容配置)                   │
│  └─ config.json (站点配置)                                │
├─────────────────────────────────────────────────────────┤
│  资源层                                                   │
│  └─ images/ (封面图片、图标等)                            │
└─────────────────────────────────────────────────────────┘
```

### 目录结构

```
youth-reading-platform/
├── index.html                 # 首页
├── category.html              # 分类列表页
├── detail.html                # 内容详情页
├── search.html                # 搜索结果页
├── about.html                 # 关于/帮助页
├── css/
│   ├── base.css              # 基础样式、CSS 变量、重置样式
│   ├── layout.css            # 布局、网格、响应式断点
│   ├── components.css        # 组件样式（卡片、按钮、导航等）
│   └── themes.css            # 主题配色
├── js/
│   ├── router.js             # 前端路由管理
│   ├── dataLoader.js         # JSON 数据加载和缓存
│   ├── searchEngine.js       # 客户端搜索实现
│   ├── renderer.js           # 内容渲染引擎
│   └── utils.js              # 工具函数（日期格式化、URL 解析等）
├── data/
│   ├── content.json          # 所有内容项元数据
│   ├── recommendations.json  # 推荐内容配置
│   └── config.json           # 站点配置
└── images/
    ├── covers/               # 内容封面图片
    ├── icons/                # 图标和标识
    └── placeholders/         # 占位图片
```

## 组件和接口设计

### 核心组件

#### 1. 路由管理器 (Router)

负责处理前端路由，实现单页应用体验。

```javascript
// router.js 接口设计
class Router {
  // 初始化路由
  init()
  
  // 注册路由规则
  // pattern: 路由模式，如 '/category/:type'
  // handler: 处理函数
  register(pattern, handler)
  
  // 导航到指定路径
  navigate(path)
  
  // 解析当前 URL 参数
  getParams()
}
```

**路由规则**:
- `/` - 首页
- `/category/:type` - 分类列表页（type: books, documentaries, tv-series, movies, kids）
- `/detail/:id` - 内容详情页
- `/search?q=:keyword` - 搜索结果页
- `/about` - 关于/帮助页

#### 2. 数据加载器 (DataLoader)

负责加载和缓存 JSON 数据。

```javascript
// dataLoader.js 接口设计
class DataLoader {
  // 加载所有内容数据
  async loadContent()
  
  // 根据 ID 获取单个内容项
  getContentById(id)
  
  // 根据类型获取内容列表
  getContentByType(type)
  
  // 加载推荐内容
  async loadRecommendations()
  
  // 加载站点配置
  async loadConfig()
  
  // 清除缓存
  clearCache()
}
```

#### 3. 搜索引擎 (SearchEngine)

实现客户端搜索功能。

```javascript
// searchEngine.js 接口设计
class SearchEngine {
  // 设置搜索数据源
  setData(contentArray)
  
  // 执行搜索
  // keyword: 搜索关键词
  // 返回: 匹配的内容项数组
  search(keyword)
  
  // 高亮搜索关键词
  // text: 原始文本
  // keyword: 要高亮的关键词
  // 返回: 带高亮标记的 HTML
  highlight(text, keyword)
}
```

**搜索算法**:
- 在标题和简介中查找关键词（不区分大小写）
- 支持多关键词搜索（空格分隔）
- 按相关度排序（标题匹配优先于简介匹配）

#### 4. 内容渲染器 (Renderer)

负责将数据渲染为 HTML。

```javascript
// renderer.js 接口设计
class Renderer {
  // 渲染内容卡片列表
  // container: 目标容器元素
  // items: 内容项数组
  renderContentList(container, items)
  
  // 渲染单个内容卡片
  // item: 内容项对象
  // 返回: HTML 字符串
  renderContentCard(item)
  
  // 渲染内容详情
  // container: 目标容器元素
  // item: 内容项对象
  renderContentDetail(container, item)
  
  // 渲染年龄分级标识
  // rating: 年龄分级值
  // 返回: HTML 字符串
  renderAgeRating(rating)
  
  // 渲染加载状态
  // container: 目标容器元素
  renderLoading(container)
  
  // 渲染错误信息
  // container: 目标容器元素
  // message: 错误消息
  renderError(container, message)
}
```

#### 5. 工具函数 (Utils)

提供通用工具函数。

```javascript
// utils.js 接口设计

// 解析 URL 查询参数
function parseQueryString(url)

// 格式化日期
function formatDate(dateString)

// 防抖函数
function debounce(func, delay)

// 图片懒加载
function lazyLoadImages(container)

// 检测设备类型
function getDeviceType()

// 本地存储操作
function setStorage(key, value)
function getStorage(key)

// 生成唯一 ID
function generateId()
```

### 页面组件

#### 导航栏组件

```html
<nav class="main-nav" role="navigation">
  <div class="nav-container">
    <a href="/" class="logo">青少年阅读平台</a>
    <ul class="nav-menu">
      <li><a href="/category/books">书籍</a></li>
      <li><a href="/category/documentaries">纪录片</a></li>
      <li><a href="/category/tv-series">电视剧</a></li>
      <li><a href="/category/movies">电影</a></li>
      <li><a href="/category/kids">少儿频道</a></li>
      <li><a href="/about">关于</a></li>
    </ul>
    <div class="search-box">
      <input type="search" placeholder="搜索内容..." aria-label="搜索">
      <button type="submit" aria-label="提交搜索">🔍</button>
    </div>
  </div>
</nav>
```

#### 内容卡片组件

```html
<article class="content-card" data-id="{id}">
  <a href="/detail/{id}" class="card-link">
    <div class="card-image">
      <img src="{cover}" alt="{title}" loading="lazy">
      <span class="age-rating age-{rating}">{rating}</span>
    </div>
    <div class="card-body">
      <h3 class="card-title">{title}</h3>
      <p class="card-description">{description}</p>
      <span class="card-type">{type}</span>
    </div>
  </a>
</article>
```

## 数据模型

### 内容项数据结构 (content.json)

```json
{
  "version": "1.0",
  "lastUpdated": "2024-01-01T00:00:00Z",
  "content": [
    {
      "id": "unique-id-001",
      "type": "book|documentary|tv-series|movie|kids",
      "title": "内容标题",
      "description": "简短描述（用于列表页）",
      "detailedDescription": "详细描述（用于详情页）",
      "cover": "/images/covers/cover-001.jpg",
      "ageRating": "6+|9+|12+|15+",
      "metadata": {
        "author": "作者名（仅书籍）",
        "publisher": "出版社（仅书籍）",
        "publishDate": "2024-01-01",
        "director": "导演（仅视频）",
        "cast": ["演员1", "演员2"],
        "duration": "120分钟（仅视频）",
        "episodes": 12,
        "language": "中文"
      },
      "links": {
        "read": "https://example.com/read",
        "watch": "https://example.com/watch"
      },
      "tags": ["标签1", "标签2"],
      "featured": true,
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-01T00:00:00Z"
    }
  ]
}
```

### 推荐内容配置 (recommendations.json)

```json
{
  "version": "1.0",
  "recommendations": [
    {
      "contentId": "unique-id-001",
      "priority": 1,
      "reason": "编辑推荐理由"
    }
  ]
}
```

### 站点配置 (config.json)

```json
{
  "version": "1.0",
  "site": {
    "title": "青少年阅读平台",
    "description": "专为青少年设计的多媒体内容平台",
    "baseUrl": "https://username.gitee.io/youth-reading-platform",
    "language": "zh-CN"
  },
  "contentTypes": [
    {
      "id": "books",
      "name": "书籍",
      "icon": "📚"
    },
    {
      "id": "documentaries",
      "name": "纪录片",
      "icon": "🎬"
    },
    {
      "id": "tv-series",
      "name": "电视剧",
      "icon": "📺"
    },
    {
      "id": "movies",
      "name": "电影",
      "icon": "🎥"
    },
    {
      "id": "kids",
      "name": "少儿频道",
      "icon": "🎈"
    }
  ],
  "ageRatings": [
    {
      "value": "6+",
      "label": "6岁以上",
      "color": "#4CAF50",
      "description": "适合6岁及以上儿童"
    },
    {
      "value": "9+",
      "label": "9岁以上",
      "color": "#2196F3",
      "description": "适合9岁及以上儿童"
    },
    {
      "value": "12+",
      "label": "12岁以上",
      "color": "#FF9800",
      "description": "适合12岁及以上青少年"
    },
    {
      "value": "15+",
      "label": "15岁以上",
      "color": "#F44336",
      "description": "适合15岁及以上青少年"
    }
  ],
  "performance": {
    "imageLazyLoad": true,
    "cacheEnabled": true,
    "cacheDuration": 3600000
  }
}
```

### 数据验证规则

1. **必填字段**: id, type, title, description, cover, ageRating
2. **ID 唯一性**: 每个内容项的 id 必须唯一
3. **类型枚举**: type 必须是预定义的五种类型之一
4. **年龄分级枚举**: ageRating 必须是 6+, 9+, 12+, 15+ 之一
5. **URL 格式**: cover 和 links 中的 URL 必须是有效格式
6. **日期格式**: 所有日期字段使用 ISO 8601 格式

